// This layout disables the default app layout (TopNav/Sidebar) for /password-change
export default function PasswordChangeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
