apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: oms-backend-pdb
  namespace: oms-staging
  labels:
    app: oms-backend
    environment: staging
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: oms-backend
