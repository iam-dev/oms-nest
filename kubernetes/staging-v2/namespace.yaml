apiVersion: v1
kind: Namespace
metadata:
  name: oms-nest-staging
  labels:
    name: oms-nest-staging
    environment: staging
    project: oms
  annotations:
    description: "OMS Staging Environment"
    managed-by: "devops-automation"
---
# Network Policy for namespace isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: oms-nest-staging-network-policy
  namespace: oms-nest-staging
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: oms-nest-staging
  - from:
    - namespaceSelector:
        matchLabels:
          name: istio-system
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: oms-nest-staging
  - to:
    - namespaceSelector:
        matchLabels:
          name: kube-system
  - to: []
    ports:
    - protocol: TCP
      port: 80
    - protocol: TCP
      port: 443
    - protocol: TCP
      port: 5432  # PostgreSQL
    - protocol: TCP
      port: 6379  # Redis