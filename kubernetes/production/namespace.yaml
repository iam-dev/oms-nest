apiVersion: v1
kind: Namespace
metadata:
  name: oms-nest-production
  labels:
    name: oms-nest-production
    environment: production
    project: oms
  annotations:
    description: "OMS Production Environment"
    managed-by: "devops-automation"
---
# Network Policy for namespace isolation
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: oms-nest-production-network-policy
  namespace: oms-nest-production
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: oms-nest-production
  - from:
    - namespaceSelector:
        matchLabels:
          name: istio-system
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: oms-nest-production
  - to:
    - namespaceSelector:
        matchLabels:
          name: kube-system
  - to: []
    ports:
    - protocol: TCP
      port: 80
    - protocol: TCP
      port: 443
    - protocol: TCP
      port: 5432  # PostgreSQL
    - protocol: TCP
      port: 25060 # DO Managed PostgreSQL
    - protocol: TCP
      port: 6379  # Redis
